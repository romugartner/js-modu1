Cree un array con un nombre asociado a mi proyecto.

Inicie con 5 elementos (string).

Agregue un nuevo elemento al final del array usando push.

Agregue uno al principio usando unshift.

Elimine el último elemento y lo guarde en una variable para mostrarlo en un mensaje: "Se ha eliminado el elemento: [nombre]".

Solicite al usuario (mediante prompt) el nombre del elemento a buscar 

Use (includes) para verificar su existencia

use indexOf para informar la posición (índice) que se encuentra.

Busque un índice específico y cambie su valor por uno nuevo implementando el método splice.

Cree una función para recorrer el array.

Dentro de la función, utilice un bucle for...of para recorrer el array y mostrar cada elemento con un formato claro

Subi los archivos a un repositorio de GitHub y comparti la siguiente URL: https://github.com/romugartner/js-modu1.git